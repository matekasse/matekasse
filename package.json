{
    "name": "matekasse",
    "version": "1.0.0",
    "description": "Awesome project developed with Mate.",
    "license": "MIT",
    "private": true,
    "workspaces": [
        "api",
        "app"
    ],
    "scripts": {
        "prepare": "husky install",
        "lint:api": "cd api && yarn ci:lint",
        "lint:app": "cd app && yarn lint",
        "lint": "yarn lint:api && yarn lint:app"
    },
    "dependencies": {
        "husky": "^7.0.1"
    }
}
