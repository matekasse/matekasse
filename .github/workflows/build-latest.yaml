name: build latest

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-latest-image-x86:
    runs-on: ubuntu-latest
    steps:
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build Docker image
        run: docker build -t ghcr.io/matekasse/matekasse:latest .
      - name: Push Docker image
        run: docker push ghcr.io/matekasse/matekasse:latest

  build-latest-image-ARM:
    runs-on: ubuntu-latest
    steps:
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build Docker image
        run: |
        docker buildx create --name arm-builder
        docker buildx build --platform linux/amd64,linux/arm64,linux/arm/v7 -t ghcr.io/matekasse/matekasse:latest -f Dockerfile_arm .
      - name: Push Docker image
        run: docker push ghcr.io/matekasse/matekasse:latest
